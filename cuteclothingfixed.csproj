<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <OutputPath>bin\$(Configuration)\Mods\mod</OutputPath>
  </PropertyGroup>

  <PropertyGroup>
    <!-- allow override via VINTAGE_STORY environment variable -->
    <GameDirectory Condition="'$(VINTAGE_STORY)' != ''">$(VINTAGE_STORY)</GameDirectory>
    <!-- Vintage Story installation path. auto-detect for cross-platform compatibility. -->
    <GameDirectory Condition="'$(GameDirectory)' == '' AND '$(OS)' == 'Windows_NT'">$(AppData)/Vintagestory</GameDirectory>
    <!-- flatpak installation on Linux -->
    <GameDirectory Condition="'$(GameDirectory)' == '' AND '$(OS)' != 'Windows_NT' AND Exists('/var/lib/flatpak/app/at.vintagestory.VintageStory')">/var/lib/flatpak/app/at.vintagestory.VintageStory/x86_64/stable/active/files/extra/vintagestory</GameDirectory>
    <!-- AUR/system installation on Linux -->
    <GameDirectory Condition="'$(GameDirectory)' == '' AND '$(OS)' != 'Windows_NT' AND Exists('/opt/vintagestory')">/opt/vintagestory</GameDirectory>
    <!-- standard Linux installation -->
    <GameDirectory Condition="'$(GameDirectory)' == '' AND '$(OS)' != 'Windows_NT' AND !Exists('/var/lib/flatpak/app/at.vintagestory.VintageStory')">$(HOME)/.config/VintagestoryData</GameDirectory>
  </PropertyGroup>

  <ItemGroup>
    <!-- exclude build tools from compilation -->
    <Compile Remove="build\**" />
    <EmbeddedResource Remove="build\**" />
    <None Remove="build\**" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="VintagestoryAPI">
      <HintPath>$(GameDirectory)/VintagestoryAPI.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="VSSurvivalMod">
      <HintPath>$(GameDirectory)/Mods/VSSurvivalMod.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="VSEssentials">
      <HintPath>$(GameDirectory)/Mods/VSEssentials.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="VSCreativeMod">
      <HintPath>$(GameDirectory)/Mods/VSCreativeMod.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(GameDirectory)/Lib/Newtonsoft.Json.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="0Harmony">
      <HintPath>$(GameDirectory)/Lib/0Harmony.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="VintagestoryLib">
      <HintPath>$(GameDirectory)/VintagestoryLib.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="protobuf-net">
      <HintPath>$(GameDirectory)/Lib/protobuf-net.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="cairo-sharp">
      <HintPath>$(GameDirectory)/Lib/cairo-sharp.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="Microsoft.Data.Sqlite">
      <HintPath>$(GameDirectory)/Lib/Microsoft.Data.Sqlite.dll</HintPath>
      <Private>False</Private>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <Content Include="assets\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="modinfo.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="modicon.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="README.md">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- post-build automation targets -->
  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Message Text="Starting post-build automation..." Importance="high" />

    <!-- read version from modinfo.json -->
    <ReadJsonFile FilePath="modinfo.json">
      <Output TaskParameter="Value" PropertyName="ModInfoJson" />
    </ReadJsonFile>

    <PropertyGroup>
      <ModVersion>$([System.Text.RegularExpressions.Regex]::Match($(ModInfoJson), '"version"\s*:\s*"([^"]+)"').Groups[1].Value)</ModVersion>
      <ZipFileName>cuteclothingfixed$(ModVersion).zip</ZipFileName>
      <ModOutputDir>$(OutputPath)</ModOutputDir>
      <ZipFilePath>$(ProjectDir)\$(ZipFileName)</ZipFilePath>
      <VsModsDir>/home/nadya/.config/VintagestoryData/Mods</VsModsDir>
    </PropertyGroup>

    <Message Text="detected mod version: $(ModVersion)" Importance="high" />
    <Message Text="creating zip: $(ZipFileName)" Importance="high" />

    <!-- create zip file -->
    <Exec Command="cd &quot;$(ModOutputDir)&quot; &amp;&amp; zip -r &quot;$(ZipFilePath)&quot; ." Condition="'$(OS)' != 'Windows_NT'" />
    <Exec Command="powershell -Command &quot;Compress-Archive -Path '$(ModOutputDir)\*' -DestinationPath '$(ZipFilePath)' -Force&quot;" Condition="'$(OS)' == 'Windows_NT'" />

    <!-- open containing folder -->
    <Exec Command="xdg-open &quot;$(ProjectDir)&quot;" Condition="'$(OS)' != 'Windows_NT'" />
    <Exec Command="explorer &quot;$(ProjectDir)&quot;" Condition="'$(OS)' == 'Windows_NT'" />

    <!-- copy to VS mods directory -->
    <Message Text="==========================================" Importance="high" />
    <Message Text="build completed! zip created: $(ZipFileName)" Importance="high" />
    <Message Text="copying to Vintage Story mods directory..." Importance="high" />

    <!-- random name to avoid overwrite -->
    <Exec Command="cp &quot;$(ZipFilePath)&quot; &quot;$(VsModsDir)/cuteclothingfixed-$(ModVersion)-$$(date +%s | sha256sum | head -c 8).zip&quot; &amp;&amp; echo &quot;Copied to $(VsModsDir) with randomized name&quot;" Condition="'$(OS)' != 'Windows_NT'" ContinueOnError="true" />

    <!-- windows version -->
    <Exec Command="copy &quot;$(ZipFilePath)&quot; &quot;$(VsModsDir)\cuteclothingfixed-$(ModVersion)-%random%%random%%random%.zip&quot; &amp;&amp; echo Copied to $(VsModsDir) with randomized name" Condition="'$(OS)' == 'Windows_NT'" ContinueOnError="true" />

    <Message Text="==========================================" Importance="high" />
  </Target>

<Import Project="build/ReadJsonFile.targets" />

</Project>